import 'package:flutter/material.dart';

class LBAdmin extends StatelessWidget {
  const LBAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
